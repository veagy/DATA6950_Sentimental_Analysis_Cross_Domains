\appendices

\section{Full HRAST Metric Rollup}
\label{app:hrast}

Table~\ref{tab:hrast_summary} lists only models for which \textbf{both} accuracy and macro-F1 were computed on the HRAST stem (two- and three-class rows from \path{output/metrics/summary.csv}). Failed loads (\path{mlp_gelu_head_ddp_*}, \path{ml_3_labels_E_ML1_LogisticRegression}, etc.) are \emph{excluded} from the table but remain in the CSV for debugging.

\subimport{tables and visualizations/tables/}{tab_hrast_summary}
